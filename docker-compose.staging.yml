services:
  db:
    container_name: todo_db_staging
    restart: always

  backend:
    container_name: todo_backend_staging
    build:
      target: prod
    restart: always

  frontend:
    container_name: todo_frontend_staging
    build:
      target: prod
    ports:
      - "8081:80"
    restart: always
